import {ScrollView, Text} from "react-native";

export default function HomeScreen() {
  return (
      <ScrollView className={"bg-amber-300"} >
          <Text className={"text-2xl"}>Hello WOrld</Text>
      </ScrollView>
  );
}
